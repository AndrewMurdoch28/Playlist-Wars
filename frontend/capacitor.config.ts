import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.daveygaming.playlistwars',
  appName: 'Playlist-Wars',
  webDir: 'dist'
};

export default config;
